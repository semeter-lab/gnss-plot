# GNSS plotting examples

This uses GNSS files from Madrigal, which can be 10+ GB.
We use boolean indexing to speed processing and minimize RAM usage.

Install libraries by:

```sh
conda install --file requirements.txt
```
